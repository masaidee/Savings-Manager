{
  "name": "keep",
  "version": "1.0.0",
  "description": "Children's Savings Management System",
  "private": true,
  "scripts": {
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npm run install-all && npm run start:all",
    "start": "npm run start:all",
    "start:all": "npm run backend:prod & npm run frontend:prod",
    "start:backend": "npm run start --prefix backend",
    "start:frontend": "npm run build --prefix frontend && npm run preview --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "setup": "npm run install-all && echo '✓ Setup complete! Run: npm run dev'",
    "lint": "echo 'No linter configured yet'",
    "test": "echo 'No tests configured yet'"
  },
  "keywords": ["savings", "children", "education", "mern"],
  "author": "",
  "license": "MIT"
}
